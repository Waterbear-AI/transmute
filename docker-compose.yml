services:
  backend:
    build: .
    ports:
      - "54718:54718"
    volumes:
      - db-data:/app/data
    environment:
      - DB_PATH=/app/data/transmute.db
    env_file:
      - .env

volumes:
  db-data:
